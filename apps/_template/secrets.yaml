# @AI_SPACE
# @purpose : Secrets de l'app (BDD, API keys, etc.)
# @logic   : ⚠️ NE JAMAIS commiter des vraies valeurs ici.
#            Options recommandées :
#              1. Sealed Secrets  → kubeseal chiffre le secret, safe à commiter
#              2. External Secrets → lit depuis Vault / AWS Secrets Manager
#            En attendant, créer le secret manuellement :
#
#   kubectl create secret generic myapp-secrets \
#     --from-literal=db-user=monuser \
#     --from-literal=db-password=monpassword \
#     --from-literal=database-url=postgresql://monuser:monpassword@postgres:5432/myapp \
#     -n myapp

# Exemple de structure (valeurs fictives) :
# apiVersion: v1
# kind: Secret
# metadata:
#   name: myapp-secrets
#   namespace: myapp
# type: Opaque
# stringData:
#   db-user: "REMPLACER"
#   db-password: "REMPLACER"
#   database-url: "postgresql://REMPLACER:REMPLACER@postgres:5432/myapp"
